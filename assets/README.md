# Bazaar native assets

`icon-only.png`, `icon-foreground.png`, `icon-background.png`, `splash.png`, and
`splash-dark.png` are generated from Bazaar-owned branding and are safe to
regenerate with `pnpm mobile:assets`. Native resource output is reviewed before
commit.
